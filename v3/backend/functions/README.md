# RETIRED reference — do not deploy

This directory is an incomplete historical Blaze design. It is intentionally absent from
`../firebase.json`, contains stubs, and is not a migration target.

The only active/deployable Functions codebase is [`../sync-fn/`](../sync-fn/). Current architecture:
[`../../docs/Architecture-V3.md`](../../docs/Architecture-V3.md). Required verification:
[`../../docs/Security-Verification-Walkthrough.md`](../../docs/Security-Verification-Walkthrough.md).
