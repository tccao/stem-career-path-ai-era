{
  "name": "cfg-v3-admin-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local admin-operated CLI for the Spark/Functions-free V3 backend. Privileged Admin-SDK ops (createUser, custom claims, grant/extend/revoke, expiry) that have no hosted home on Spark. Run by the admin with a service-account key, or against the emulator.",
  "engines": { "node": ">=20" },
  "scripts": {
    "make-admin": "node make-admin.mjs",
    "grant": "node grant.mjs",
    "extend": "node extend.mjs",
    "revoke": "node revoke.mjs",
    "expiry": "node expiry-sweep.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "ulid": "^2.3.0",
    "zod": "^3.23.0"
  }
}
