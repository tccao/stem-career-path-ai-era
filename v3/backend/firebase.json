{
  "//": "Spark / Functions-free. Deploy target on Spark: `firebase deploy --only firestore:rules`. The functions/ dir is a Blaze reference and is NOT deployed (Spark cannot deploy Cloud Functions). No Storage on Spark.",
  "firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": true }
  }
}
